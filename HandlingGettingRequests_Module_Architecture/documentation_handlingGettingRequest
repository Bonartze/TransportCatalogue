
explicit FillingCatalogue(RouteImitation::TransportCatalogue &) - конструктор, инициализирует каталог

void FillFileData(const std::string &) - заполненение входного потока данными, которые лежат в файле по названию(по параметру)

void GetParsedJsonData() - парсинг данных, которые лежат во входном потоке и сохранение их в соответствующую переменную(зависимость иежду модулями приведена на диаграмме)

void FillingData() - заполнение транспортного каталога с помощью обработанных данных

void DisplayResultRequest(const std::string &) - отрисовка результата с помощью модуля render (зависимость иежду модулями приведена на диаграмме)
